<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/input"
        android:title="@string/input"
        android:icon="@mipmap/ic_bus_white_24dp"
        app:showAsAction="ifRoom|collapseActionView"
        app:actionViewClass="android.support.v7.widget.SearchView"
        />
    <item android:id="@+id/followers"
        android:title="followers"
        app:showAsAction="never"
        />
    <item android:id="@+id/photos"
        android:title="photos"
        app:showAsAction="never"
        />
    <item android:id="@+id/login"
        android:title="login"
        app:showAsAction="never"/>
</menu>